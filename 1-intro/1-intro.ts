let user = {name: 'John', age: 30};

console.log(user.name); // John
console.log(user.age); // 30
//console.log(user.email)

export {}; // Make sure this file is treated as a module.export ./1-intro/1-intro.ts;
